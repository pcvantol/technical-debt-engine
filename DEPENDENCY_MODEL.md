# Dependency model

Each dependency has canonical identifier, display name, ecosystem, package manager, declared version, resolved version where available, lock state, scope, license, and source. Generation 1 discovery reports declared dependencies and explicit `DECLARED` lock state only.
