# Reference Consumer Contract

The first reference consumer invokes only the released CLI and consumes its JSON evidence, canonical configuration, schemas and exit codes. It records the pinned TDE runtime/schema/capability versions and preserves evidence immutably.

Aggregation projects repository, capability, trend and qualification summaries without modifying source evidence. Historical technical debt is informational evidence and does not become a release gate by itself.
