# Comparison configuration

Top-level `baseline` configuration supports `location`, with `.tde/baselines` as the default. The CLI supports `--baseline-location`, `tde baseline --name NAME`, and `tde compare --baseline NAME`. Retention and multiple/release/branch baseline selection are future work.
