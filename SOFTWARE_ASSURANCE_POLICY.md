# Software Assurance Policy

Required repository, configuration, schema, dependency declaration and clean-working-tree checks must pass. Missing dependency locking, workflow validation, or release artifacts produce explicit warnings. Any required integrity failure produces `FAIL`.
