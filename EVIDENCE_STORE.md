# Evidence Store

The Evidence Store persists validated canonical Runtime evidence independently of storage technology. Generation 1 uses an immutable filesystem backend; Runtime owns no persistence.
