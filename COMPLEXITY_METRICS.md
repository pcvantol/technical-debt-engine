# Complexity metrics

Canonical metric namespace: `complexity.cyclomatic.average`, `.median`, and `.maximum`, all score-valued repository aggregates. Future reserved concepts include p90, p95, distribution, file/symbol complexity, cognitive complexity, nesting depth, essential complexity, and Halstead; none are implemented by this increment.
