# Limitations model

Limitations are structured records with ID, capability, scope, impact, description, cause, affected metrics/findings, qualification impact, and resolution guidance. They are first-class evidence, not free-form notes.

Examples include unavailable analyzers, unsupported language constructs, partial coverage, excluded generated code, unavailable source-control identity, and language-specific unavailable metrics. See `domain.schema.json` definitions.
