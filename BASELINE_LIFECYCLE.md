# Baseline lifecycle

1. Create a baseline from validated canonical evidence.
2. Persist it once in the configured location.
3. Discover it by name or JSON path.
4. Compare future canonical evidence against it.

Baselines are never updated, replaced, or mutated. A new baseline is a new immutable identity.
