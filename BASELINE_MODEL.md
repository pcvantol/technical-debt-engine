# Baseline model

A baseline is an immutable, versioned snapshot of validated canonical evidence. It identifies repository, candidate, runtime and schema version, creation timestamp, policy version, evidence digest, and source evidence. A baseline name can never be overwritten.
