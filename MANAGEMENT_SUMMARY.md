# Management summary

## Current management decision — supersedes earlier active roadmap direction

`0.2.0` is published and qualified. Generation 1 is complete. DJConnect is now
the primary product and TDE is the supporting engineering tool that supplies
the minimum reliable assessment function DJConnect pipelines need. Generation
2 is therefore the compact **TDE 1.0 DJConnect Enablement Program**: coverage
completion first, then minimal dependency health and basic security evidence,
a selected-consumer CI pilot, and one integrated `1.0.0` qualification and
release. No per-capability public release is planned; post-1.0 is
maintenance-first. [ADR-0064](architecture/adr/ADR-0064-djconnect-enablement-program.md)
and [PRODUCT_BACKLOG.md](PRODUCT_BACKLOG.md) are canonical for this decision.

The remainder of this document is preserved historical management context and
does not override the current decision above.

Technical Debt Engine is being established as an independent engineering product that measures, normalizes, qualifies, and reports maintainability and technical debt across projects and languages.

R1-4H Release Publication Completion — PyPI Publication Repair decision: `PYPI_PUBLICATION_BLOCKED`. PR #83 replaced only the unavailable pinned PyPI Action with current verified PyPA `release/v1` commit `cef221092ed1bacb1cc03d23a2d87d1d172e277b`; its GHCR container manifest exists and publication contract tests pass. Protected run [29531471511](https://github.com/pcvantol/technical-debt-engine/actions/runs/29531471511) re-verified the preserved certified bundle and authorization, but the all-target workflow created a local tag then remote push rejected immutable existing tag `0.1.0`. It stopped before Docker or PyPI, leaving previous published targets unchanged and PyPI absent. Completion requires a narrowly scoped resumable PyPI-only path that re-verifies completed identities and publishes only the preserved Python artifacts plus completion evidence.

R1-4G Internal Release Publication decision: `INTERNAL_RELEASE_PARTIALLY_COMPLETED`. Protected workflow [29529932503](https://github.com/pcvantol/technical-debt-engine/actions/runs/29529932503) re-verified the authorized preserved bundle, then created immutable tag `0.1.0` at certified candidate `223ccfe4b3646f1907ee7e2d7a8c07e8989badd7`, GitHub Release, and Docker Hub OCI index `sha256:aa648019045a442a0dbce029ee11ecb15c7755d845205fa8f07467e0faf18679` with qualified amd64/arm64 manifests. PyPI was not published: the pinned `pypa/gh-action-pypi-publish` container could not be pulled from GHCR (`manifest unknown`), so the workflow stopped before publication evidence creation. No `latest` tag was published. Completion requires a narrowly scoped publication-workflow action-availability repair and preserved-artifact completion without rebuild.

R1-4F Human Release Authorization decision: `CURRENT_MAINLINE_RELEASE_AUTHORIZATION_RECORDED`. Immutable authorization `authorization.sha256.09973d239287053808740f38bb83b102146cc5a3ae943c5b1148f571ef2e4631` binds sole-maintainer approver `pcvantol`, timestamp, certified candidate `223ccfe4b3646f1907ee7e2d7a8c07e8989badd7`, version `0.1.0`, bundle/checksum, `internal-release`, repaired manual publication workflow, and independent Git tag, GitHub Release, PyPI, and Docker Hub approvals. Structural validation and retrieved-bundle preflight passed. No tag, release, upload, Docker push, or workflow dispatch occurred. The sole next prompt is R1-4G — Internal Release Publication.

R1-4E decision: `PUBLICATION_WORKFLOW_IDENTITY_REPAIRED_AND_CANDIDATE_CERTIFIED`. PR #79 repaired the protected publication job with explicit repository-local Git tagger identity `Technical Debt Engine Release Automation <technical-debt-engine-release[bot]@users.noreply.github.com>`. Isolated regression tests prove missing identity blocks before tag creation and approved identity tags the selected SHA. Non-publishing dry-run [29527658608](https://github.com/pcvantol/technical-debt-engine/actions/runs/29527658608) passed. Fresh mainline candidate `223ccfe4b3646f1907ee7e2d7a8c07e8989badd7` passed Runtime Qualification `QUALIFIED`, Policy `PASS_WITH_WARNINGS`, Software Assurance `PASS`, Trusted Delivery `PASS`, Release Qualification `RELEASE_QUALIFIED` / `READY`, and Release Certification `RELEASE_CERTIFIED` in [run 29527704042](https://github.com/pcvantol/technical-debt-engine/actions/runs/29527704042). Bundle `bundle.sha256.f80926d7d5fb978c9da4f997230e76f9833f2d607a258553d92a807db22a2836` is artifact `8387371267`, retained to 2026-10-14 and retrieved/checksum-verified without rebuilding. It is unpublished and requires new human authorization.

R1-4D Internal Release Publication decision: `INTERNAL_RELEASE_BLOCKED`. Protected run [29526820939](https://github.com/pcvantol/technical-debt-engine/actions/runs/29526820939) passed both checksum-bound certified-bundle preflights and immutable authorization validation, but failed before tag creation because the runner lacks Git committer identity for the annotated tag. The GitHub Release, Docker Hub, PyPI, and publication-evidence steps were skipped; verification confirms no tag, GitHub Release, PyPI version, or Docker tag exists. The release remains unpublished. A workflow identity repair changes the candidate-to-main workflow boundary, so a fresh candidate is required before retry.

R1-4B Human Release Authorization decision: `HUMAN_RELEASE_AUTHORIZATION_RECORDED`. Immutable record `authorization.sha256.73d47d6991e39983669fe77468feb919107658978d881aa3c941d5780aa334bc` binds sole-maintainer approver `pcvantol`, timestamp, current candidate `04b39c51e2e36a5ac70059f2c030e7cadd37dbe0`, version `0.1.0`, certified bundle/checksum, Actions retention, `internal-release`, and explicit approvals for Git tag, GitHub Release, PyPI, and Docker Hub targets. Structural validation and retrieved-bundle preflight passed. No tag, release, upload, Docker push, or publication dispatch occurred. The sole next prompt is R1-4C — Internal Release Publication.

R1-GOV-5 Single Maintainer Internal Release Authorization Policy decision: `SINGLE_MAINTAINER_RELEASE_POLICY_OPERATIONAL`. GitHub identifies `pcvantol` as repository owner and sole direct collaborator; `internal-release` has that required reviewer and permits self-review. The canonical policy accepts this configuration only for the verified single-maintainer model, while requiring an explicit immutable authorization record, candidate/bundle verification, target binding, protected-environment approval, and publication evidence. If multiple maintainers exist, independent approval and self-review prevention become mandatory. The current certified bundle remains unpublished and requires a fresh authorization record in R1-4B.

R1-4A Create and Certify Current Mainline Release Candidate decision: `CURRENT_MAINLINE_RELEASE_CANDIDATE_PARTIALLY_CERTIFIED`. Successful non-publishing Actions [run 29483960813](https://github.com/pcvantol/technical-debt-engine/actions/runs/29483960813) selected current `main` commit `04b39c51e2e36a5ac70059f2c030e7cadd37dbe0`, verified its ancestry, built two byte-identical wheel/source-distribution pairs, created a non-published `linux/amd64` and `linux/arm64` OCI archive, and produced Runtime Qualification `QUALIFIED`, Policy `PASS_WITH_WARNINGS`, Software Assurance `PASS`, Trusted Delivery `PASS`, Release Qualification `RELEASE_QUALIFIED` / `READY`, and Release Certification `RELEASE_CERTIFIED`. The checksum-bound bundle `bundle.sha256.fe7a81f7daa9fafbf40a031c7988ad3e7b1b00dda94e4e91facc4e30352b4ec1` is Actions artifact `8369651393`, retrievable through 2026-10-14 and independently retrieved/verified without rebuilding. No tag, GitHub Release, PyPI upload, Docker push, or `latest` tag occurred. The decision is partial only because the existing `internal-release` Environment has one reviewer and allows self-review, which does not meet the documented publication contract. The sole next prompt is R1-4B — Human Release Authorization.

R1-GOV-4 Immutable GitHub Action Reference Parser Repair decision: `IMMUTABLE_ACTION_PARSER_OPERATIONAL`. Software Assurance now parses and normalizes `owner`, `repository`, optional `path`, and complete commit SHA for both step-level `uses:` and job-level reusable-workflow references. The root cause was `.github/workflows/internal-release-publish.yml` job `publish`, step `Publish the certified Python distributions using Trusted Publishing`: its valid `pypa/gh-action-pypi-publish@6733eb7d741f0b11ec6a39b58540dab7590f9b7d` reference has no leading `-`, which the former line regex required. The repaired parser recognizes all 40 workflow references, rejects branches, tags, missing/short SHAs, expressions, variables, and matrix-derived references, and preserves fail-closed policy. Validation: 98 tests pass; Software Assurance is `PASS` against the two retained independent package builds from successful non-publishing run `29451595432`; a Trusted Delivery consumer validation is `PASS` with the repaired assurance evidence. No Runtime behavior, workflow source, Release Candidate, tag, or publication changed. The sole recommended next prompt is R1-4A — Create and Certify Current Mainline Release Candidate.

R1-3C Human Release Authorization decision: `HUMAN_RELEASE_AUTHORIZATION_BLOCKED`; [PR #73](https://github.com/pcvantol/technical-debt-engine/pull/73) merged at `73e198d`. The immutable authorization record binds approver `pcvantol`, timestamp, candidate `3fda62e`, version `0.1.0`, certified bundle/checksum, workflow, and explicit approvals for the Git tag, GitHub Release, PyPI, and Docker Hub targets. Bundle preflight remains ready, but the GitHub API returns `404` for `internal-release`; required reviewers and credentials therefore cannot be verified. No Environment was configured, and no tag, release, PyPI upload, Docker upload, or `latest` tag occurred.

R1-3B Internal Release Publication Infrastructure decision: `PUBLICATION_INFRASTRUCTURE_OPERATIONAL` pending PR review. The manual-only canonical workflow retrieves only the retained certified bundle, verifies its checksum, candidate SHA, Qualification and Certification, validates an authorization assertion's structure, and emits preflight evidence without rebuilding or publishing. A protected `internal-release` Environment contract documents reviewers, least-privilege permissions, PyPI Trusted Publishing with a scoped-token fallback, Docker Hub credentials, immutable version-only Docker tags, and GitHub Release evidence. No tag, GitHub Release, PyPI package, Docker image, or `latest` tag was created. R1-3C owns human authorization and any approved non-dry-run dispatch.

R1-3A Mainline Internal Release Candidate retry decision: `MAINLINE_INTERNAL_RELEASE_CANDIDATE_CERTIFIED`. Candidate `3fda62e` passed the exact-mainline ancestry gate, deterministic package/OCI build, Runtime Qualification (`QUALIFIED`), Policy (`PASS_WITH_WARNINGS`), Software Assurance and Trusted Delivery (`PASS`), Release Qualification (`RELEASE_QUALIFIED`), and Release Certification (`RELEASE_CERTIFIED`) in [run 29451595432](https://github.com/pcvantol/technical-debt-engine/actions/runs/29451595432). The checksum-bound bundle `bundle.sha256.e0c12c31b0ecf4b0bc6a9a4054717ed4d449c70ff90af9fc917f0ac87c6deeef` was retrieved and verified without rebuilding. No tag or external publication occurred; the sole next prompt is R1-3B human authorization and Internal publication.

R1-GOV-3 Candidate Source-Branch Identity decision: `CANDIDATE_SOURCE_BRANCH_IDENTITY_CORRECTED`. Release Qualification now consumes the same validated `TDE_CANDIDATE_SOURCE_BRANCH` fallback as Software Assurance and Trusted Delivery for detached exact-SHA checkouts. The regression test reproduces that topology and the full 91-test suite passes. No candidate was recreated, certified, bundled, tagged, or published; a fresh R1-3A attempt is deferred until this correction merges.

CI-1 GitHub CI Tooling Refresh: upgrades the hash-verified pip bootstrap to `26.1.2`, moves `actions/setup-python` to v6.3.0, and moves Docker QEMU/Buildx setup actions to v4.2.0. The existing hash-locked package build chain is intentionally unchanged because those versions are release-producing inputs; their upgrade requires a separate reproducibility review.

R1-3A Mainline Internal Release Candidate decision: `MAINLINE_INTERNAL_RELEASE_CANDIDATE_BLOCKED`. Candidate `a507838` is an exact merged mainline snapshot and its deterministic wheel/sdist and non-published multi-platform OCI build passed in [run 29450643140](https://github.com/pcvantol/technical-debt-engine/actions/runs/29450643140). Release Qualification correctly failed closed because its detached-checkout candidate branch was empty while Trusted Delivery used the validated `main` branch. Certification, bundle preservation, and publication did not occur. The bounded next increment is R1-GOV-3 source-branch identity correction.

R1-GOV-2 Release Architecture Evolution decision: `MAINLINE_SNAPSHOT_RELEASE_MODEL_IN_PROGRESS`. PR #66 is merged at `0d7fea6`, not draft. Its prior candidate `2d6132061807a433178a1ababc1709340cb937de` is a sibling of main with shared parent `a07271b`; it is preserved as `SUPERSEDED_NON_MAINLINE_CANDIDATE` and cannot be published. New candidates must be exact existing mainline SHAs, validated by ancestry, built and certified from that SHA, and published only from a retained bundle after explicit human authorization. No external publication, final tag, or promotion occurs in this increment.

R1-2B Docker-integrated Release Candidate decision: `SUPERSEDED_NON_MAINLINE_CANDIDATE`. Candidate `2d6132061807a433178a1ababc1709340cb937de` passed the hosted non-publishing multi-architecture OCI workflow in [run 29446629544](https://github.com/pcvantol/technical-debt-engine/actions/runs/29446629544). Its verified checksum-bound bundle is retained for 90 days; no Docker Hub or other registry publication occurred. PR #66 merged at `0d7fea6`; the candidate is its sibling rather than a main ancestor, so it cannot be promoted.

R1-GOV-1 Release Engineering Governance decision: `CERTIFIED_CANDIDATE_PUBLICATION_BOUNDARY_ESTABLISHED`. The canonical publication boundary is Certified Candidate → Administrative Merge → Human Authorization → Publication. The certified candidate `5932411` remains valid after administrative R1-1 merge `df8a36b`; its eight changed paths are rolling status, release governance/evidence, and prompt history only. Publication must classify every candidate-to-main commit, stop for any product/package/dependency/build/artifact/workflow change, and tag/publish the certified candidate rather than current main. No Runtime, capability, qualification, certification, policy, artifact, tag, or publication changed. The recommended next prompt remains `R1-2 — Human Release Authorization & Internal Publication`.

R1-1 Release Candidate Refresh & Re-Certification decision: `CURRENT_RELEASE_CANDIDATE_CERTIFIED`. Fresh evidence binds the current main commit `5932411201556be628fb5ca93912a26f95b9d424` to two byte-identical non-published wheel/sdist builds, checksum-bound provenance, Runtime Qualification `QUALIFIED`, Policy `PASS_WITH_WARNINGS`, Software Assurance `PASS`, Trusted Delivery `PASS`, Release Qualification `RELEASE_QUALIFIED` / `READY`, and Release Certification `RELEASE_CERTIFIED` (`release-certification.sha256.1faddeb93d49956055e59449bd70d2f897493650ad7b9d2362cb81c4e7996a2d`). The canonical candidate record is `release/current-candidate-r1-1.json`; no tag, GitHub Release, Docker image, package publication, or human authorization occurred. The sole recommended next prompt is `R1-2 — Human Release Authorization & Publication`.

P1-12 Release Policy Remediation decision: `RELEASE_POLICY_REMEDIATED`. [PR #62](https://github.com/pcvantol/technical-debt-engine/pull/62) merged at `958ce5b`; the subsequent wheel dependency update [PR #55](https://github.com/pcvantol/technical-debt-engine/pull/55) merged at `23887b6`. P1-12 reduced the release candidate's blocking complexity without changing policy thresholds or suppressing findings. The CLI dispatcher was split into focused command handlers, Runtime Qualification and release-evidence integrity checks were simplified, and schema validation was decomposed. Fresh two-build dogfooding selected Code Size and Complexity: Runtime Qualification was `QUALIFIED`, policy `PASS_WITH_WARNINGS` (maximum complexity 23, below the blocking threshold 25), Software Assurance and Trusted Delivery `PASS`, Release Qualification `RELEASE_QUALIFIED` / `READY`, and Release Certification `RELEASE_CERTIFIED` (`release-certification.sha256.f2e6a4f4bfea030c77e3b0e6c07d043ec3b76cb2001b21a768df7e344bff7afe`). 83 tests passed. No tag, release, publication, or human approval occurred.

P1-11 Release Evidence Qualification decision: `RELEASE_EVIDENCE_PARTIALLY_QUALIFIED`. Branch `codex/p1-11-release-evidence-qualification`, implementation commit `39f6331`, and [reviewable PR #61](https://github.com/pcvantol/technical-debt-engine/pull/61) add explicit `--release-capability` selection to `tde release-qualify`. Release Qualification now executes the selected capabilities, persists immutable `release-evidence` with Runtime Qualification, Policy, Software Assurance, Trusted Delivery, artifacts, candidate and Release Qualification cross-references, and `tde certify` validates its identity/digest without duplicating Runtime or Policy logic. Dogfooding two byte-identical TDE builds selected Code Size and Complexity: Runtime Qualification was `QUALIFIED`, assurance and Trusted Delivery `PASS`, and Release Evidence `release-evidence.sha256.cd339f8e89050d368c96ea7eef1ae0b4b66e663a64a37ef41c24554c57cc9846` persisted; Policy returned `FAIL`, so Release Qualification was `RELEASE_BLOCKED`/`NOT_READY` and certification `RELEASE_NOT_CERTIFIED` (`release-certification.sha256.9b7feba92ea91c4147caf47f0be27a4bc812486c4f2a81111ba08e0498bdff1b`). 83 tests passed and `git diff --check` passed. No tag, GitHub Release, package publication, or release occurred. Deferred work: remediate the selected candidate's policy failure, then recertify.

P1-10 Release Certification decision: `RELEASE_NOT_CERTIFIED`. Branch `codex/p1-10-release-certification`, implementation commit `e379715c417619babf163a9b52cbadfe087fd675`, and [draft PR #60](https://github.com/pcvantol/technical-debt-engine/pull/60) add `tde certify`, which consumes canonical Release Qualification JSON and produces an integrity-bound Certification Report without regenerating Software Assurance or Trusted Delivery. Dogfooding an installed wheel against two byte-identical candidate builds produced qualification `RELEASE_QUALIFIED` / `READY` and certification evidence `release-certification.sha256.66cd5ff226d087603831b780bf5e6146dc282e27738fc13f5702834dab58f377`; the certification decision is `RELEASE_NOT_CERTIFIED` because Runtime Qualification was `BLOCKED` and Policy evidence was `NOT_APPLICABLE` for the default unselected-capability candidate. 79 tests and `git diff --check` passed. No tag, GitHub Release, or package publication occurred. Deferred work: produce qualifying Runtime and Policy evidence for the selected release capability, then recertify.

P1-10 decision: `RELEASE_QUALIFIED`. Branch `codex/p1-10-release-qualification`, implementation commit `25966ab655225e14bd9aadca708ae1abe6bfde3e`, and [draft PR #59](https://github.com/pcvantol/technical-debt-engine/pull/59) add `tde release-qualify`, which generates canonical manifest and release evidence from two byte-identical wheel/sdist builds, Software Assurance `assurance.sha256.48f639f3df08549399248cb8bc6949ad0eb3eef68cae8c33621af76d47f14838`, and Trusted Delivery `trusted-delivery.sha256.53f168d5dc61163b0e88124b00f059867e3560f2445ffa1799f1a82cd187a66a`. Dogfooding returned `READY`; 76 tests and `git diff --check` passed. No tag, release, certification, or publication occurred. Release Certification is the recommended next prompt.

G2-GOV-6 decision: `POST_MERGE_ENGINEERING_STATE_RECONCILIATION_OPERATIONAL`. Branch `codex/g2-gov-6-post-merge-reconciliation`, reconciliation commit `c32175ca615419f63318d9896162cc727bdd8691`, and [draft PR #58](https://github.com/pcvantol/technical-debt-engine/pull/58) reconcile objective GitHub evidence that [P1-9 PR #57](https://github.com/pcvantol/technical-debt-engine/pull/57) merged into `main` at `9a612192cdf0777b7c7758d068a47339f8771fb8` on 2026-07-15 05:11:59 UTC, with both accepted commits and no blocking review. Rolling current-state documents now reflect that merge while the immutable P1-9 Freeze-Point record remains unchanged. The lifecycle now distinguishes `REVIEWABLE_FROZEN`, `MERGED_UNRECONCILED`, and `MERGED_RECONCILED`; future substantive prompts reconcile an expected post-merge transition administratively in their own PR, while material inconsistency remains fail-closed. Validation: objective GitHub verification, merge containment in `main`, documentation checks, and `git diff --check`; no product tests were required because no product implementation changed. No product implementation, release, tag, certification, or publication occurred. Deferred work is Release Qualification. The sole recommended next recovery prompt is `P1-10` — Operational Release Qualification.

P1-9 decision: `TRUSTED_DELIVERY_OPERATIONAL`. Branch `codex/p1-9-trusted-delivery`, implementation commit `65cbb9d010d7c6156e18a355f66b6b0cc42c9e0b`, and merged [PR #57](https://github.com/pcvantol/technical-debt-engine/pull/57) operationalize `tde trusted-delivery`. The command validates candidate SHA/repository/branch, a schema-versioned manifest, checksum-bound reproducible artifacts and provenance, immutable least-privilege workflow sources, validated Runtime evidence, and a consumed Software Assurance reference. Dogfooding TDE on the clean implementation candidate produced canonical evidence `trusted-delivery.sha256.386e1c28777348b7cfb8e6b02334a9fd5149b4670041b8d08c16a1bbea1b96bf` with `PASS_WITH_WARNINGS` solely because no release-stage manifest or independent artifact directories were supplied. Validation: 75 deterministic tests passed and `git diff --check` passed. No release, certification, tag, or publication was created. Known limitation and deferred work: evaluate a real release candidate with its external manifest and independently reproducible artifacts in Release Qualification, the recommended next recovery prompt.

P1-7 decision: `BUILD_REPRODUCIBILITY_FOUNDATION_PARTIALLY_OPERATIONAL`. Branch `codex/p1-7-build-reproducibility-foundation`, candidate commit `5a99556e63de51cca29da6face552e018084a7cf`, and [reviewable PR #54](https://github.com/pcvantol/technical-debt-engine/pull/54) add an exact, SHA-256-hash-locked build-tool policy, exact setuptools backend pin, canonical wheel/sdist builder, checksum manifest, and versioned build provenance. Two local builds from the same candidate produced byte-identical wheel and source-distribution artifacts. Isolated installations of both artifacts invoked only the installed `tde` CLI and completed CLI/runtime, Code Size, Complexity, Policy, baseline, comparison, Query, and report dogfooding. The least-privilege immutable-action GitHub workflow is defined for pull requests and manual dispatch, uploads candidate evidence, and performs neither publication nor release. Workflow run `29367776918` failed before the second build because the first setuptools build changed a tracked egg-info manifest; repairing source-tree isolation is deferred to the next prompt. No release or publication has occurred. The recommended next recovery prompt is the isolated workflow repair after review and merge.

Generation 1 creates the product foundation: capability boundaries, canonical evidence and qualification concepts, a CLI contract, roadmap, governance, and release policy. It intentionally contains no runtime code, analyzers, or releases. DJConnect is the first production reference consumer through stable contracts only.

The repository now uses a mandatory incremental engineering workflow: every canonical prompt is a small, traceable increment that concludes in exactly one independently reviewable pull request. Merging remains an explicit decision after review. This establishes predictable governance before the AI-Native Engineering Method is introduced.

The AI-Native Engineering Method is now established as the canonical engineering constitution. It provides a mandatory session bootstrap, source hierarchy, ADR process, agent roles, program model, and explicit human authority over merge and release. Runtime implementation remains intentionally unstarted.

The Canonical Runtime Architecture now defines the execution pipeline and strict separation between runtime orchestration and adapter measurement. It establishes canonical normalization, validation, qualification, immutable evidence, reporting, and exit-status contracts while preserving project, language, platform, and vendor independence. No runtime, adapter, analyzer, or CLI implementation has been created.

Prompt 4 establishes the versioned domain and evidence schema contracts that bind those architectural stages. JSON Schema, a schema registry, neutral fixtures, and deterministic development-only validation now separate domain entities, measurements, findings, evidence, validation, qualification, reporting, configuration, and adapter provenance. No executable TDE runtime capability has been introduced.

Prompt 5 establishes the canonical capability contract and qualification policy model. A registry now governs discovery, lifecycle, ownership, dependencies, configuration, validation, evidence, reporting, versioning, and compatibility for independently evolvable capabilities. The registry records planned Generation 1 capabilities only; no capability behavior has been implemented.

Prompt 6 establishes the Adapter SDK as the only supported extension mechanism between the runtime and native analyzers. The SDK specifies isolated adapter behavior, registry-only discovery, local Generation 1 loading, configuration, independent versioning, and explicit compatibility. Registry entries are planned declarations only; no adapter or analyzer invocation exists.

Prompt 7 implements the first executable Runtime Foundation. It provides a stable Python API for generic orchestration, execution context, empty registries, runtime validation, empty-capability evidence, runtime-ready qualification, and runtime summary reporting. It intentionally adds no CLI, capability, adapter, or analyzer behavior.

Prompt 8 implements the first executable `tde` CLI as a thin Runtime consumer. It provides command metadata, generated help, version reporting, configuration loading, logging, human/JSON presentation, and canonical exit codes. Only generic `validate` and `inspect` invoke the Runtime Foundation; all capability-facing routes remain explicitly not implemented.

Prompt 9 delivers the first Code Size vertical slice with `cloc 2.10` as an explicitly installed native analyzer. It is validated through Runtime and CLI integration, canonical normalized evidence, deterministic classification, and automated fixtures. The result remains observational and macOS-validated only; cross-platform qualification and configurable thresholds remain future work.

Prompt 10 adds the Complexity reference capability with Python/Radon 6.0.1. It demonstrates a second analyzer adapter normalizing into the same evidence model, with planned analyzer selections for the remaining language roadmap. It is validated, not yet cross-platform or multi-language qualified.

Prompt 11 adds derived Maintainability without a native analyzer, consuming canonical Code Size and Complexity evidence only.

Prompt 12 adds declarative Dependency Health discovery for common manifest formats without package-manager execution or network access.

Prompt 13 makes qualification operational through a standalone, versioned Policy Engine. Policies are dynamically discovered JSON contracts with explicit compatibility; their decisions and triggered rules are preserved as evidence. Qualification now consumes only those policy decisions, keeping capability-specific thresholds outside the Runtime.

Prompt 14 transforms the product from point-in-time assessment to evolution-aware assessment. Immutable baseline snapshots and canonical evidence comparisons now detect metric deltas, finding regressions, improvements, and capability support; Policy Engine integration retains a separate decision layer.

Prompt 15 adds continuous engineering intelligence through normalized, repository-local trend evidence. History, direction, moving averages, capability evolution, and policy history are exposed through `tde trend` while qualification remains owned by the Policy Engine.

Prompt 16 adds the canonical read-only Query Engine, used by Runtime and `tde query` to project canonical engineering evidence without analyzer or evidence mutation.

Prompt 17 adds an immutable, storage-independent Evidence Store with filesystem persistence and history through `tde store` and `tde history`.

Prompt 18 establishes the Capability Execution Engine as the single execution coordinator. Runtime now orchestrates only; `tde run` uses planned, sequential, isolated capability execution with execution evidence.

Prompt 19 adds Runtime Qualification: evidence-only completeness, confidence, limitations and capability support assessment through `tde qualify`, without any consumer release decision.

Prompt 20 completes the first Generation 1 platform qualification. The platform is operational for continued internal engineering but partially qualified: reporting, persisted-query integration, empty-evidence confidence hardening, and broader analyzer qualification remain explicit gaps. No release or package has been created.

Prompt 21 performs the first formal platform certification. It concludes `PLATFORM_NOT_CERTIFIED`: the platform is suitable for controlled internal iteration, but empty-evidence qualification, incomplete persisted-query/reporting paths, bounded analyzer qualification, and legacy execution ownership must be resolved before it becomes a certified canonical foundation.

Prompt 22 establishes the package-independent Platform Release Engineering architecture: immutable release planning, artifacts, profiles, manifests, and evidence with GitHub Actions as future execution engine. It creates no package, workflow, publication, or release.

Prompt 23 adds evidence-based, fail-closed Software Assurance through `tde assure`. It evaluates repository, configuration, schema, documentation, dependency, workflow and artifact integrity without implementing Trusted Delivery or release publication.

Prompt 24 adds Trusted Delivery through `tde trusted-delivery`: immutable candidate, manifest, workflow, artifact and runtime-evidence validation with Software Assurance as informational evidence. It creates no workflow, artifact, package or release.

Prompt 25 performs the first Release Qualification. The Generation 1 candidate is `RELEASE_BLOCKED`: it has immutable identity and a canonical manifest, but no immutable workflow, dependency provenance, release artifacts/checksums, or platform certification. No publication occurred.

Prompt 26 performs the first formal Release Certification. It concludes `RELEASE_NOT_CERTIFIED`: Release Engineering is architecturally coherent, but workflow execution, dependency provenance, artifact integrity, reporting and platform certification remain objective blockers.

Prompt 27 performs the first local operational release dry run. It creates one non-published wheel candidate and checksum, but is `RELEASE_DRY_RUN_BLOCKED` because GitHub Actions, dependency provenance, source/reproducibility evidence and certifications remain unavailable.

Prompt 28 validates a local Internal Release 0.1.0 wheel and isolated installation, but concludes `INTERNAL_RELEASE_BLOCKED`: no internal destination, source archive, executable/evidence bundle, or release certification is available. Nothing was published.

Prompt 29 records an Operational Burn-In over the installed internal wheel. Three local runs each for TDE, empty and multi-language repositories produced deterministic normalized evidence, but cross-platform, long-running and release-certification gaps leave the result `OPERATIONAL_BURN_IN_BLOCKED`.

Prompt 30 establishes the DJConnect reference-consumer contract but is `REFERENCE_CONSUMER_INTEGRATION_BLOCKED`: no released TDE CLI is available to pin and no single DJConnect repository has been selected. No consumer repository was modified.

Prompt 31 closes Generation 1 strategy and establishes Generation 2. The frozen Generation 1 foundations are extended through exactly three programs: Core Runtime, Platform Evolution and Innovation Lab. No implementation or Runtime Architecture change is introduced.

Prompt 32 independently audited current main and establishes the current product truth as `TDE_PRODUCT_PARTIALLY_OPERATIONAL`. A local Python wheel and `tde` console entry point can be installed; help, version, inspection, local filesystem baseline/store/history, and direct Runtime Code Size execution are demonstrable. The public `assess`/`run` CLI routes did not execute requested capabilities in the installed audit, Runtime Qualification incorrectly qualifies empty evidence, Query does not read persisted Evidence Store records, and no release has been published. The recovery sequence therefore starts with a truthful, installed CLI Code Size vertical slice before any Generation 2 expansion or DJConnect integration.

Recovery Prompt P0-1 delivers that first vertical slice. The installed `tde assess --capability code-size` command now executes through the canonical Runtime and registry-backed Execution Engine, invokes `cloc`, produces canonical measurements and execution evidence, and returns qualified evidence only when a capability and adapter actually executed. Empty execution and missing capability or adapter evidence now fail closed in Runtime Qualification. This resolves the first P0 recovery item only; the platform remains unreleased and partially operational.

G2-GOV-1 establishes the Generation 2 Engineering Method operating model without changing product implementation. Engineering now starts from current `main` and observable operational reality, with `ENGINEERING_STATUS.md` as the replace-in-full current handoff and immutable prompt archives as repository-native continuity. Historical prompt order remains context, not the source of current engineering priorities.

P1-1 completes Code Size as the first end-to-end installed-CLI slice on the audited macOS host: configuration, analyzer validation, canonical metrics/evidence, qualification, storage, query and report output are connected through the Runtime. The slice is not yet cross-platform qualified or released.

G2-GOV-3 establishes Engineering Method v2.2 without altering product implementation. Prompt ownership is explicit: one prompt owns one objective, one engineering increment, and one reviewable Pull Request. A draft PR may carry the mandatory finalization records, but only reviewable state triggers the immutable freeze boundary; merge remains a human decision. New work discovered after Freeze is Deferred Work for a subsequent increment. The repository now has a canonical hygiene policy and `.gitignore`; safe macOS-artifact cleanup leaves no tracked or untracked `.DS_Store` file while retaining engineering evidence, release evidence, and fixtures. The earlier G2-GOV-2 finalization-record gap remains deferred to a separate governance prompt.

P1-2 decides `CODE_SIZE_VERTICAL_SLICE_OPERATIONAL` on objective macOS-host evidence. Branch `agent/code-size-evidence-store` delivers automatic immutable persistence for installed-CLI Code Size assessment and integrity-verified persisted Query and report consumption in PR #41. Validation comprises 51 passing tests, `git diff --check`, and a newly built/installed wheel dogfood run against TDE itself: `assess` qualified and stored evidence `sha256:b223ad30e894e288b7eac5a1a82d5ee0b2939ba53598ec795c011ce4796e61c6`, persisted Query returned 1,006 metrics, and Markdown report rendered from that record. `cloc 2.10+` and macOS-only execution evidence remain limitations. The recommended next recovery prompt is `P1-3` for cross-platform Code Size analyzer qualification.

Complexity recovery P1-2 extends the same installed-package path to Python/Radon 6.0+: canonical repository, language, file and symbol cyclomatic measurements, distributions, thresholded findings, native-output hashes, persisted Query and a Complexity report. The result is operational on the macOS audit host, with non-Python and cross-platform analyzer qualification deferred.

P1-3 introduces a bounded, read-only GitHub-hosted qualification workflow for the existing Python/Radon Complexity slice. It builds one candidate wheel, installs it in isolated Linux, macOS and Windows environments, captures redacted qualification records and fails closed if their normalized analytical output differs. The outcome remains pending workflow execution; no release or non-Python analyzer work is included.

P1-2 Complexity is frozen for review in PR #43 with decision `COMPLEXITY_VERTICAL_SLICE_OPERATIONAL`. The installed-wheel dogfood run against TDE produced 611 persisted metrics and a qualified Runtime record; its assessment exit was correctly blocked by TDE's own critical complexity findings, while persisted Query and report succeeded.

G2-GOV-4 records the historical-branch incident transparently and decides `HISTORICAL_BRANCH_CLOSURE_BLOCKED`. The three commits are classified without cherry-pick or history rewrite: `6f4d60c` is superseded by P1-2 / PR #41, `d1aa3eb` is rejected as an invalid post-PR #38 finalization, and `da548a8` is preserved as deferred governance evidence. PR #38, #39, and #41 are merged and no open PR references either historical branch, but the two remote branches still exist. Mandatory cleanup verification therefore does not authorize local deletion. `G2-GOV-5` must receive explicit remote-branch-removal authority before closure can continue.

PD-1 decision: `CANONICAL_DEPLOYMENT_STRATEGY_ESTABLISHED`. Branch `codex/pd-1-canonical-deployment-strategy` establishes [Deployment Strategy](docs/product/DEPLOYMENT_STRATEGY.md) at candidate commit `1a15f57` in [PR #47](https://github.com/pcvantol/technical-debt-engine/pull/47). Validation is documentation-only: the canonical product layers, Generation 1 targets, distribution products, consumer model, release profiles, installation priority, GitHub Action, Docker, Python-library positioning and deployment lifecycle are documented; `git diff --check` passes; no implementation, Runtime, capability, workflow, artifact, publication, or release changes are included. The strategy documents PyPI, Homebrew, Docker, GitHub Releases, native package managers, GitHub Action and Python Library responsibilities while preserving the current limitation that none is deployed or published. Deferred work is release packaging/provenance/workflow implementation, Generation 2 package-manager and IDE evaluation, and Generation 3 service research. Determine the next Product Definition prompt only after review and merge.

PD-2 decision: `CANONICAL_PERSISTENCE_ARCHITECTURE_ESTABLISHED`. Branch `codex/pd-2-canonical-persistence-architecture` establishes [Persistence Architecture](docs/product/PERSISTENCE_ARCHITECTURE.md) at candidate commit `8c7f6ae` in [PR #48](https://github.com/pcvantol/technical-debt-engine/pull/48). Validation is documentation-only: the canonical storage hierarchy, JSON authority, filesystem role, SQLite indexing boundary, cloud/local-first evolution, retention model and Query-only consumer model are documented; roadmap and backlog references are aligned; `git diff --check` passes; no Runtime, implementation, capability, test, workflow, artifact, publication or release changes are included. Known limitations: SQLite, retention automation, cloud persistence and migrations are not implemented. Deferred work is SQLite qualification, retention lifecycle policy and future organization/cloud evaluation. Determine the next Product Definition prompt only after review and merge.

PD-3 decision: `CANONICAL_DASHBOARD_ARCHITECTURE_ESTABLISHED`. [PR #49](https://github.com/pcvantol/technical-debt-engine/pull/49) documents a Query-only, read-only Dashboard presentation layer; no Runtime, implementation, deployment, analyzer, artifact, publication or release change is included. The local server, CLI integration, Docker/self-hosting and cloud dashboard remain deferred.

PD-4 decision: `PRODUCT_STRATEGY_ALIGNMENT_ESTABLISHED`. [PR #50](https://github.com/pcvantol/technical-debt-engine/pull/50) aligns product navigation and Strategy with Deployment, Persistence and Dashboard Product Definitions. Validation is documentation-only with `git diff --check`; no Runtime or implementation changes. Future Release, API, Cloud, Organization and Authentication definitions remain deferred.

P1-4 decision: `CODE_SIZE_CROSS_PLATFORM_QUALIFIED`. Branch `codex/p1-4-code-size-cross-platform-qualification` qualifies candidate commit `ee8c625bc3081f73f8f729a4216c75ba5fb50b85` in [PR #46](https://github.com/pcvantol/technical-debt-engine/pull/46). GitHub Actions run [29363505649](https://github.com/pcvantol/technical-debt-engine/actions/runs/29363505649) built one wheel (`sha256:59c3da9595954d61a4f02c7eab8b6b6cceaeb4dbe91216223d607d49e7782968`), installed it on Ubuntu, macOS, and Windows for Python 3.11 and 3.13, and provisioned checksum-verified `cloc 2.10`. All six persisted-evidence records passed storage, retrieval, identity/integrity, persisted Query, persisted report, missing/unsupported/timeout fail-closed checks, and installed-wheel dogfooding; their canonical analytical comparison is equivalent. Local isolated-package validation passed 60 tests and `git diff --check`. Known limitations: only Code Size is qualified by this increment; no release, publication, or deployment occurs. Deferred work: non-Python Complexity qualification and action-runtime maintenance. Recommended next prompt is determined only after PR review and merge.

P1-5 decision: `POLICY_ENGINE_OPERATIONAL`. Branch `codex/p1-5-policy-qualification` delivers candidate commit `293d61fa70a9ed654aca87988a95c25b4702a2b9` in draft [PR #51](https://github.com/pcvantol/technical-debt-engine/pull/51). Policy qualification evaluates real Code Size and Complexity evidence, persists policy identity/version, thresholds, measured values, triggered rules, affected evidence, decision reason and qualification reference, and retrieves it only through persisted Query. Runtime Qualification consumes Policy Engine output without reimplementing thresholds; CLI mappings for `PASS`, `PASS_WITH_WARNINGS`, `FAIL`, `BLOCKED`, and `NOT_APPLICABLE` are deterministic. Validation: 65 tests pass, `tools/run_policy_qualification.py` dogfoods TDE with both capabilities and persisted Query, `git diff --check` passes, and a least-privilege immutable-reference workflow runs on pull requests and manual dispatch. Known limitations: Python Complexity is the only qualified Complexity analyzer path; organization/cloud/release policy providers are deferred. No release or publication occurs. Recommended next recovery prompt is determined after review and merge.
