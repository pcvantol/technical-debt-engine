# Dependency configuration

Enable with `tde assess --capability dependency-health`. Ignored packages, versions, thresholds, and vulnerability providers are reserved for future schema-backed configuration.
