# Prompt 14 dogfooding

`prompt-14.json` is an immutable baseline generated by `tde baseline` for this repository on 2026-07-14. `tde compare` against that baseline produced `comparison.227a317f87a2dbe0` with `VALID` compatibility, no metric or finding deltas, and a `NOT_APPLICABLE` policy decision because no capabilities were enabled for this smoke assessment.

The stored baseline is evidence, not a release baseline or retention mechanism.

Prompt 15 ran `tde trend` against `prompt-14`. It produced `trend.4491598e9ebbca00`, a rolling history with one baseline reference, no measurement/finding limitations, and a stable `NOT_APPLICABLE` qualification history for the no-capability smoke assessment.
