# Dependency Health capability

Dependency Health `0.1.0` provides declarative inventory discovery without executing package-manager tools. It normalizes Python `requirements.txt`/`pyproject.toml` and JavaScript `package.json` into one dependency model. NuGet, SwiftPM, Conan, vcpkg, locks, transitive resolution, outdated checks, vulnerabilities, and licenses are future work.
