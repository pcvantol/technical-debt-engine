# Execution model

The engine discovers enabled capabilities, resolves Generation 1 dependencies, and executes isolated capability implementations sequentially. Parallel execution, cancellation, retries, and incremental execution remain future work.
