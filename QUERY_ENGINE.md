# Query Engine

The Query Engine is the single read-only interface over canonical evidence. Runtime, CLI, reports, and future consumers use its versioned results; it never invokes analyzers or modifies evidence.
