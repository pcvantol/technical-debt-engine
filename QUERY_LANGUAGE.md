# Query language

Version `1.0.0` is machine-readable and independent of CLI syntax. A query names a `resource` and may use `filter`, `aggregate`, `groupBy`, `sort`, `projection`, `offset`, and `limit`.
