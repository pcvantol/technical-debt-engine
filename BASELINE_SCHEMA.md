# Baseline schema

A baseline is an immutable snapshot of validated canonical evidence. It stores baseline ID, source evidence ID, repository/candidate identity, schema/runtime versions, creation time, policy context, and integrity digest together with the source evidence snapshot. It is never modified after creation.

See [`schemas/baseline.schema.json`](schemas/baseline.schema.json) and [`fixtures/baseline.json`](fixtures/baseline.json).
