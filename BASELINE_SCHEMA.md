# Baseline schema

A baseline is an immutable reference to validated canonical evidence. It stores baseline ID, source evidence ID, repository/candidate identity, schema/runtime versions, creation time, policy context, optional label, and integrity digest. It never duplicates or mutates source evidence.

See [`schemas/baseline.schema.json`](schemas/baseline.schema.json) and [`fixtures/baseline.json`](fixtures/baseline.json).
