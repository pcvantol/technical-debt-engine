# Comparison Engine

The Comparison Engine consumes only validated canonical current evidence and an immutable baseline snapshot. It detects metric deltas, finding transitions, capability comparison support, limitations, regressions, and improvements. It makes no qualification decision; comparison evidence is supplied to the Policy Engine.
