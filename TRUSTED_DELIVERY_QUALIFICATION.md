# Trusted Delivery Qualification

`PASS` meets all available checks. `PASS_WITH_WARNINGS` records bounded pre-release limitations. `FAIL` has a required candidate or evidence failure. `BLOCKED` is reserved for unavailable required validation.
