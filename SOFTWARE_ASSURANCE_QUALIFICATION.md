# Software Assurance Qualification

`PASS` has no limitations; `PASS_WITH_WARNINGS` has known bounded limitations; `FAIL` has a required integrity failure; `BLOCKED` is reserved for unavailable required evidence.
