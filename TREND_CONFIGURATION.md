# Trend configuration

The `trend` configuration object supports `historyDepth`; CLI `--history-depth` selects the rolling history depth. The baseline location is shared with the Baseline Engine. Retention and configurable aggregation are reserved for future versions.
