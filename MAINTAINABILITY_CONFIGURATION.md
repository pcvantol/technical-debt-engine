# Maintainability configuration

Enable through `tde assess --capability maintainability`. The derived capability requires Code Size and Complexity internally and does not accept analyzer configuration. Thresholds, weights, and dependency overrides are future schema-backed configuration.
