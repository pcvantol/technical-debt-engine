# Comparison model

Compatible comparisons require the same repository, runtime version, and schema version. Numeric metrics produce numeric and percentage deltas, ratio metrics also produce ratio deltas, and distribution deltas remain explicitly unavailable until a distribution metric is implemented. Findings are introduced, resolved, unchanged, severity-increased, or severity-decreased.
